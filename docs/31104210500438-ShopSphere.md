# ShopSphere — Project Links

**Student ID:** 31104210500438

| Resource | URL |
|---|---|
| Application (frontend) | _fill in after deploying — e.g. `https://shopsphere-<you>.vercel.app`_ |
| Backend API | _fill in — e.g. `https://shopsphere-backend-<you>.vercel.app`_ |
| Backend health check | _same host as above +_ `/api/health` |
| Review service | _fill in — e.g. `https://shopsphere-reviews-<you>.vercel.app`_ |
| Review service health check | _same host as above +_ `/api/health` |
| Repository | _fill in — e.g. `https://github.com/<you>/shopsphere`_ |
| Architecture diagram | `docs/31104210500438-ShopSphere-Architecture-Diagram.svg` (in the repo above) |
| Cloud classification | `docs/31104210500438-ShopSphere-Cloud-Classification.md` |
| ADR | `docs/31104210500438-ShopSphere-ADR.md` |
| Rollback plan | `docs/31104210500438-ShopSphere-Rollback-Plan.md` |
| UptimeRobot status page | _fill in after registering the health-check endpoints_ |

## Before submitting

1. Fill in every blank above once deployed.
2. Make sure the **repository** is set to Public, or Private with your
   instructor/grader added as a collaborator — "public" alone doesn't help
   if code is what needs reviewing, but the rubric's visibility rule means
   whatever you choose must actually be reachable by whoever grades this.
3. Publish **this document itself** somewhere with anyone-with-the-link
   access — a public GitHub file (this is one, if the repo is public), a
   shared Google Doc, or a Notion page set to public. The rubric requires
   this document specifically to be link-shareable, independent of
   whether the repo is public.
