apiVersion: v1
kind: Service
metadata:
  name: shopsphere-frontend
  namespace: aws-simulation
spec:
  selector:
    app: shopsphere-frontend
    env: aws-simulation
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
