apiVersion: v1
kind: Service
metadata:
  name: shopsphere-backend
  namespace: aws-simulation
spec:
  selector:
    app: shopsphere-backend
    env: aws-simulation
  ports:
    - port: 5000
      targetPort: 5000
  type: ClusterIP
